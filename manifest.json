{
  "manifest_version": 2,
  "name": "Double Shift Delete",
  "description": "Remove elements by hovering your mouse over something and double clicking the shift key.",
  "version": "0.0.1",
  "permissions": [ "*://*/*" ],
  "content_scripts": [{
    "matches": ["<all_urls>"],
    "js": ["index.js"],
    "run_at": "document_end"
  }]
}
